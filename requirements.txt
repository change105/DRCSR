# Tested with Python 3.10, PyTorch 2.1+ (CUDA 12.x), single RTX 4090D.
# RecBole is included locally in recbole/ — do NOT pip install recbole.
torch>=2.0.0
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0
tqdm>=4.62.0
tensorboard>=2.8.0
PyYAML>=5.4.0
